@page
@model CrystalByRiya.Areas.Admin.Pages.AddOns.CreateModel

        @{
        ViewData["Title"] = "Create";
        }
        
        <h1>Create</h1>
        
    <h4>AddOn</h4>
    <hr />
    <div class="row">
    <div class="col-md-4">
    <form method="post">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="AddOn.ProductId" class="control-label"></label>
                <input asp-for="AddOn.ProductId" class="form-control" />
                <span asp-validation-for="AddOn.ProductId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="AddOn.AddOnName" class="control-label"></label>
                <input asp-for="AddOn.AddOnName" class="form-control" />
                <span asp-validation-for="AddOn.AddOnName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="AddOn.Price" class="control-label"></label>
                <input asp-for="AddOn.Price" class="form-control" />
                <span asp-validation-for="AddOn.Price" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
